import {Route} from '@angular/router';
import {AddCustomerComponent} from './add-customer.component';

export const addCustomerRouting: Route[] = [
    {
        path     : '',
        component: AddCustomerComponent
    }
];
